schemes:
  - http
  - https
securityDefinitions:
  Bearer:
    type: apiKey
    name: Authorization
    in: header
paths:
  /collectives:         # path of the user from your endpoint
    post: # endpoint request type (post request)
      tags:
        - Collective
      security:
        - Bearer: []
      summary: Create collective
      produces:
      - application/json
      parameters:
      - name:       # name of parameter passed
        type: string
      - in: body
        name: create collective
        description: It enables a user to create collective
        required: false
        schema:
          $ref: '#/definitions/collectiveCreate'
      responses:
        201:
          description: An object with a new collective
          content:
            application/json:
              schema:
                $ref: '#/definitions/collectiveCreate'
        401:
          description: Unauthorized users
    get: # endpoint request type (post request)
      tags:
        - Collective
      security:
        - Bearer: []
      summary: Get All collectives
      produces:
      - application/json
      responses:
        200:
          description: An object with a collective values
        401:
          description: Unauthorized users
  /collectives/{id}:         # path of the user from your endpoint
    get: # endpoint request type (post request)
      tags:
        - Collective
      security:
        - Bearer: []
      summary: Get collective By Id
      produces:
      - application/json
      parameters:  # name of parameter passed
        - in: path
          name: id
          description: It enables a user to fetch a collective
          required: false
          type: string
      responses:
        200:
          description: An object with a collective values
        401:
          description: Unauthorized users
          # path of the user from your endpoint
    delete: # endpoint request type (post request)
      tags:
        - Collective
      security:
        - Bearer: []
      summary: Delete collective By Id
      produces:
      - application/json
      parameters:  # name of parameter passed
        - in: path
          name: id
          description: It enables a user to delete a collective
          required: false
          type: string
      responses:
        200:
          description: Delete Confirmation
        401:
          description: Unauthorized users
    put: # endpoint request type (post request)
      tags:
        - Collective
      security:
        - Bearer: []
      summary: Update collective
      produces:
        - application/json
      parameters:
        - name:       # name of parameter passed
          type: string
        - in: body
          name: update collective
          description: It enables a user to update collective
          required: false
          schema:
            $ref: '#/definitions/collectiveUpdate'
      responses:
        200:
          description: An object with a collective
        401:
          description: Unauthorized users
definitions:        # Schema definition for the request body
  collectiveCreate:
    type: object
    properties:
      collectiveName:
        type: string
      users:
        type: array
        items:
          type: object
      channels: 
         type: array
         items: object
      collectivePhoto: 
          type: string
  collectiveUpdate:
    type: object
    properties:
      collectiveName:
        type: string
      users:
        type: array
        items:
          type: object
      channels: 
         type: array
         items: object
      collectivePhoto: 
          type: string

